# 投研助手 - Python 依赖清单
# 核心约束: numpy<2 以确保 pandas/pyarrow 二进制兼容性
# 环境: Python 3.10+

# ── 数据处理 ──
numpy>=1.24.0,<2.0.0          # 锁定 1.x, 避免 _ARRAY_API 兼容性崩溃
pandas>=2.0.0,<3.0.0
pyarrow>=15.0.0,<18.0.0

# ── 金融数据源 ──
yfinance>=0.2.0,<2.0.0
akshare>=1.10.0,<2.0.0
# tushare>=1.4.0              # 可选, 需注册Token
# fredapi>=0.5.0              # 可选, 需FRED API Key

# ── MCP Server ──
fastmcp>=3.0.0,<4.0.0

# ── 网络与解析 ──
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.0

# ── 可视化 (可选) ──
# matplotlib>=3.7.0
# seaborn>=0.13.0
# plotly>=5.0.0
